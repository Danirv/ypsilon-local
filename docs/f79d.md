# Runxin F79D profile

The F79D profile is the device knowledge currently used by Ypsilon. Field ids,
read codecs and known legacy write encodings are defined in
`runxin/fields.py`; `runxin/f79d.py` turns that catalogue into frames and decoded
state.

## Evidence legend

- **A** — present in the legacy WaterDevice field/codec implementation;
- **O** — value/behavior observed on the project's real Ypsilon/F79D data;
- **HW** — write encoding directly exercised against physical local hardware;
- **CW** — a value change was observed through the legacy cloud application.

`A` is evidence of application support, not proof that every F79D firmware or
rebranded device implements the field identically.

## Field catalogue

| ID | Name | Read codec | Known write codec | Meaning / notes | Evidence |
|---:|---|---|---|---|---|
| 1 | `deviceModel` | u8 | — | Device model; tested value 9 | A,O |
| 2 | `language` | u8 | u8 | Device language setting | A |
| 3 | `deviceTimeScheme` | u8 | — | Time-display/scheme setting | A |
| 4 | `currentTime` | time HM | time HM | Controller clock | A |
| 5 | `washInitiationTime` | time HM | time HM | Wash start time | A |
| 6 | `continuousWaterTime` | u8 | u8 | Continuous-flow safety time | A |
| 7 | `flowRateOff` | u16 BE | u16 BE | Flow shutoff threshold, hundredths | A,O,HW |
| 8 | `waterVolumeUnit` | u8 | — | Volume/flow unit selector | A,O |
| 9 | `workPattern` | u8 | u8 | Work-pattern setting | A |
| 10 | `regeneratingTriggerTime` | time HM | time HM | Regeneration trigger time | A |
| 11 | `flowRate` | u16 BE | — | Instantaneous flow raw counter, hundredths | A,O |
| 12 | `systemCloseReason` | u16 LE | — | Valve/system close reason | A |
| 13 | `washingIncreaseNumber` | u8 | u8 | Legacy wash-increase parameter | A |
| 14 | `backWashIntervalNumber` | u8 | u8 | Backwash interval parameter | A |
| 15 | `backWashTime` | duration | time HM | Backwash configured duration | A |
| 16 | `backWashTimeRemaining` | duration | — | Backwash remaining | A |
| 17 | `absorbSaltSlowWashTime` | duration | time HM | Brine draw / slow wash duration | A |
| 18 | `absorbSaltTimeRemaining` | duration | — | Brine/slow-wash remaining | A |
| 19 | `saltTankRefillTime` | duration | time HM | Brine tank refill duration | A |
| 20 | `saltTankRefillTimeRemaining` | duration | — | Refill remaining | A |
| 21 | `washTime` | duration | time HM | Fast-rinse/wash duration | A |
| 22 | `washCountdownTime` | duration | — | Wash remaining | A |
| 23 | `maximumRegenerationIntervalDay` | u8 | u8 | Maximum regeneration interval; treated as a diagnostic limit | A |
| 24 | `outRelayMode` | u8 | u8 | Output-relay mode | A |
| 25 | `regenerationAlarmNumber` | u16 LE | u16 LE | Regeneration alarm count/parameter | A |
| 26 | `resinVolume` | u8 | — | Resin volume, observed in litres | A,O |
| 27 | `clockChipFault` | bool | — | Clock chip fault | A |
| 28 | `multiplePositionSignalFault` | bool | — | Multiple position signals fault | A |
| 29 | `noPositionSignalFault` | bool | — | No position signal fault | A |
| 30 | `memoryErrorFault` | bool | — | Memory fault | A |
| 31 | `saltShortageAlarm` | bool | — | Salt shortage alarm | A |
| 32 | `resinReplacementReminder` | bool | — | Resin replacement reminder | A |
| 33 | `reminderFlags` | flags | — | Low byte salt reminder; high byte filter reminder | A |
| 34 | `station` | u8 | u8 | Current valve phase; app also writes system mode here | A,O |
| 35 | `residualWaterProduction` | volume pair | — | Remaining treatment capacity; continuation 36 | A,O |
| 36 | `residualWaterProductionContinuation` | continuation | — | Continuation for field 35 | A,O |
| 37 | `dailyWaterConsumption` | volume pair | — | Daily cumulative consumption; continuation 38 | A,O |
| 38 | `dailyWaterConsumptionContinuation` | continuation | — | Continuation for field 37 | A,O |
| 39 | `averageWeeklyWaterConsumption` | volume pair | — | Weekly average; continuation 40 | A,O |
| 40 | `averageWeeklyWaterConsumptionContinuation` | continuation | — | Continuation for field 39 | A,O |
| 41 | `periodicWaterProduction` | volume pair | — | Configured/cycle treatment quantity; continuation 42 | A,O |
| 42 | `periodicWaterProductionContinuation` | continuation | — | Continuation for field 41 | A,O |
| 43 | `saltAddition` | u8 | u8 | Salt addition setting, kg in legacy UI | A,O,CW |
| 44 | `operationDay` | u8 | — | Configured regeneration interval in time/day mode | A,O |
| 45 | `remainingDay` | u8 | — | Remaining-day countdown in time/day mode | A,O |
| 46 | `regenerationPattern` | u8 | u8 | 0 flow/volume, 1 time/day | A,O |
| 47 | `rawWaterHardness` | u16 LE | u16 LE | Raw-water hardness, mg/L | A,O |
| 48 | `absorbSaltMode` | u8 | u8 | Brine/salt absorption mode | A |
| 49 | `vacationPattern` | bool | u8 | Vacation/holiday mode | A |
| 50 | `saltDissolutionRemainingTime` | u8 | — | Salt-dissolving remaining time/state value | A,O |
| 51 | `pauseRemainingTime` | u8 | — | Pause remaining time/state value | A,O |
| 52 | `filterMaterialWorkingDay` | u16 LE | u16 LE | Configured filter-media service interval, not elapsed age | A,O |

## Volume-pair reconstruction

Fields 35, 37, 39 and 41 require the immediately following continuation field.
The formula recovered from the legacy codec and retained for compatibility is:

```text
value = (continuation_high
         + continuation_low * 100
         + base_high * 10000) / 100
```

The base field's low byte is not part of that formula. Offline regression tests
pin the current behavior so a refactor cannot silently change historical values.

## Regeneration semantics

`regenerationPattern` is model-dependent UI state in the legacy application:

- `0`: flow/volume based;
- `1`: time/day based.

For the project's device, flow/volume mode is the normal configuration.
`residualWaterProduction` is therefore remaining treatment capacity, not water
physically stored in a tank. `operationDay` / `remainingDay` are mainly relevant
in time/day mode.

## Safe Home Assistant write surface

The codec knows encodings for more fields than the integration exposes. The
administrator-only raw service is currently restricted to:

```text
4, 5, 6, 7, 10, 34, 43, 47, 49
```

Normal entities expose an even smaller, purpose-specific set with ranges and
physical read-back. Do not broaden this list just because `write_codec` exists
in the catalogue; a syntactically valid mechanical setting can still be unsafe.

## Field 52 polling

The normal state query is fields 1..51. Field 52 is read separately and cached
for one hour by the Ypsilon adapter. That is an integration/device polling
optimization, not part of the generic Runxin transport contract.
