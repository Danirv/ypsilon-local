[English](f79d.md) | [Español](f79d.es.md) | [Català](f79d.ca.md)

# Runxin F79D profile

The F79D profile is the device knowledge currently used by Ypsilon. Field ids, read codecs and known legacy write encodings live in `runxin/fields.py`; `runxin/f79d.py` turns that catalogue into frames and decoded state.

For the evidence trail behind the legacy WaterDevice application, real-device observations and the v2.6.1 safety decisions, also see [`waterdevice-audit.md`](waterdevice-audit.md).

## Evidence legend

- **A** — present in the legacy WaterDevice field/codec implementation;
- **O** — value/behavior observed on the project's real Ypsilon/F79D data;
- **HW** — local write exercised end-to-end against the physical controller and confirmed through an independent fresh local read-back;
- **CW** — value change observed through the vendor/cloud application path.

`A` proves how the recovered application codec represents a field. It does **not** prove that every current controller firmware accepts that write locally. A transport ACK without matching fresh read-back is not `HW` evidence.

## Field catalogue

| ID | Name | Read codec | Known legacy write codec | Meaning / notes | Evidence |
|---:|---|---|---|---|---|
| 1 | `deviceModel` | u8 | — | Device model; tested value 9 | A,O |
| 2 | `language` | u8 | u8 | Device language | A |
| 3 | `deviceTimeScheme` | u8 | — | Time-display/scheme | A |
| 4 | `currentTime` | time HM | time HM | Controller clock | A,O,HW |
| 5 | `washInitiationTime` | time HM | time HM | Wash start time; read-only in HA | A |
| 6 | `continuousWaterTime` | u8 | u8 | Continuous-flow safety time | A,O,HW |
| 7 | `flowRateOff` | **u16 LE** | **u16 LE** | Flow shutoff threshold, hundredths; legacy codec is LE | A,O |
| 8 | `waterVolumeUnit` | u8 | — | Volume/flow unit selector | A,O |
| 9 | `workPattern` | u8 | u8 | Work-pattern enum; read-only in HA | A,O |
| 10 | `regeneratingTriggerTime` | time HM | time HM | Regeneration trigger time | A,O,HW |
| 11 | `flowRate` | **u16 BE** | — | Instantaneous flow, hundredths; app explicitly reverses bytes | A,O |
| 12 | `systemCloseReason` | u16 LE | — | Valve/system close reason | A |
| 13 | `washingIncreaseNumber` | u8 | u8 | Legacy wash-increase parameter | A |
| 14 | `backWashIntervalNumber` | u8 | u8 | Backwash interval parameter | A |
| 15 | `backWashTime` | duration min/sec | duration min/sec | Backwash duration | A |
| 16 | `backWashTimeRemaining` | duration min/sec | — | Backwash remaining | A |
| 17 | `absorbSaltSlowWashTime` | duration min/sec | duration min/sec | Brine draw / slow rinse duration | A |
| 18 | `absorbSaltTimeRemaining` | duration min/sec | — | Brine/slow-rinse remaining | A |
| 19 | `saltTankRefillTime` | duration min/sec | duration min/sec | Brine refill duration | A |
| 20 | `saltTankRefillTimeRemaining` | duration min/sec | — | Brine refill remaining | A |
| 21 | `washTime` | duration min/sec | duration min/sec | Fast-rinse duration | A |
| 22 | `washCountdownTime` | duration min/sec | — | Fast-rinse remaining | A |
| 23 | `maximumRegenerationIntervalDay` | u8 | u8 | Maximum regeneration interval | A |
| 24 | `outRelayMode` | u8 | u8 | Output-relay mode | A |
| 25 | `regenerationAlarmNumber` | u16 LE | u16 LE | Legacy regeneration alarm parameter | A |
| 26 | `resinVolume` | u8 | — | Resin volume, observed in litres | A,O |
| 27 | `clockChipFault` | bool | — | Clock chip fault | A |
| 28 | `multiplePositionSignalFault` | bool | — | Multiple position signals fault | A |
| 29 | `noPositionSignalFault` | bool | — | No position signal fault | A |
| 30 | `memoryErrorFault` | bool | — | Memory fault | A |
| 31 | `saltShortageAlarm` | bool | — | Legacy UI meaning: **low brine concentration** | A |
| 32 | `resinReplacementReminder` | bool | — | Resin replacement reminder | A |
| 33 | `reminderFlags` | flags | — | Low byte salt reminder; high byte filter reminder | A |
| 34 | `station` | u8 | u8 | Current valve phase/system mode | A,O; mechanical write not generally HW-verified |
| 35 | `residualWaterProduction` | volume pair | — | Remaining treatment capacity; continuation 36 | A,O |
| 36 | continuation | continuation | — | Continuation for 35 | A,O |
| 37 | `dailyWaterConsumption` | volume pair | — | Cumulative consumption for the current day; resets at day boundary | A,O |
| 38 | continuation | continuation | — | Continuation for 37 | A,O |
| 39 | `averageWeeklyWaterConsumption` | volume pair | — | Controller-reported weekly average; **not** the app's week-history total | A,O |
| 40 | continuation | continuation | — | Continuation for 39 | A,O |
| 41 | `periodicWaterProduction` | volume pair | — | Treatment/cycle capacity; not a cumulative consumption meter | A,O |
| 42 | continuation | continuation | — | Continuation for 41 | A,O |
| 43 | `saltAddition` | u8 | u8 | Amount of salt added/bookkept, kg; **not** remaining salt level | A,O,HW,CW |
| 44 | `operationDay` | u8 | — | Configured interval in time/day mode | A,O |
| 45 | `remainingDay` | u8 | — | Remaining-day countdown | A,O |
| 46 | `regenerationPattern` | u8 | u8 | 0 metered/volume, 1 time/day | A,O |
| 47 | `rawWaterHardness` | u16 LE | u16 LE | Raw-water hardness, mg/L | A,O,HW |
| 48 | `absorbSaltMode` | u8 | u8 | Brine/salt absorption mode | A |
| 49 | `vacationPattern` | bool | u8 | Readable vacation flag. Legacy codec can encode 1/0, but direct local write failed physical confirmation on the tested G6 | A,O; **no HW** |
| 50 | `saltDissolutionRemainingTime` | u8 | — | Salt-dissolution remaining time, minutes | A,O |
| 51 | `pauseRemainingTime` | u8 | — | Pause-1 remaining time, minutes | A,O |
| 52 | `filterMaterialWorkingDay` | u16 LE | u16 LE | Filter-media service interval | A,O |

## Endianness correction in 2.6.0

The legacy WaterDevice codec serialises field 7 with its generic little-endian helper. Field 11 is different: WaterDevice explicitly reverses that two-byte array before decoding, therefore its wire representation is big-endian.

Earlier Ypsilon versions used BE for field 7 in both write and read paths. That implementation could self-confirm a write because the same incorrect interpretation was used in both directions. Version 2.6.0 follows the legacy codec and withdraws the previous `HW` evidence for field 7 until the corrected LE implementation is physically revalidated.

## Unit-dependent volume-pair reconstruction

Fields 35, 37, 39 and 41 consume the immediately following continuation field. Let the relevant bytes be:

```text
base field:          [id, base_low, base_high]
continuation field:  [id+1, next_low, next_high]
```

The legacy application constructs the logical three-byte value as:

```text
[next_high, next_low, base_high]
```

and then interprets it according to `waterVolumeUnit`:

- unit `0` or `1`: 24-bit little-endian integer
  `next_high + (next_low << 8) + (base_high << 16)`;
- unit `2`: decimal/base-100 packing
  `next_high + next_low*100 + base_high*10000`, presented by Ypsilon in cubic metres with `/100` scaling.

If the unit field or continuation field is missing, Ypsilon returns `None` rather than fabricating a plausible zero.

## Water/statistics semantics validated for 2.6.1

### Daily consumption — fields 37–38

A real exported history from the tested G6 shows the field rising during each day and resetting near the day boundary. In Home Assistant this is therefore exposed as `TOTAL_INCREASING`: a reset starts a new meter cycle and is not interpreted as negative consumption.

### Controller weekly average — fields 39–40

The live controller value and the vendor application's week-by-week history bars are different quantities. The application has a separate statistics path for the historical weekly bars. Field 39 is therefore named **Controller weekly average consumption** and intentionally has no Home Assistant `state_class`.

### Treatment capacity per cycle — fields 41–42

The legacy UI treats this as a treatment/cycle capacity/configuration quantity. It is not a cumulative water meter, so it intentionally has no `state_class`.

Older integration versions briefly generated long-term statistics for fields 39 and 41. Home Assistant may offer to delete those obsolete statistics after upgrade; that cleanup does not remove the entities or their normal recorder history.

## Flow units

The legacy UI labels field 11 as:

- unit 0: `gpm`;
- unit 1: `Lpm` (`L/min`);
- unit 2: `m³/h`.

Only unit 2 has been calibrated end-to-end on the project's physical Ypsilon G6. The field-7 Home Assistant control therefore remains available only in unit code 2.

## Salt semantics

Field 43 is the legacy `addSalt` setting. The vendor UI presents it as a 0–100 kg **amount of salt added**. Local SET/read-back has been physically verified and the same value has also been observed changing through the vendor path.

It is not a physical level sensor. Ypsilon therefore does not decrement it after regeneration and does not treat it as salt remaining. Low-brine and add-salt/reminder conditions are provided by separate controller fields 31 and 33.

## Vacation semantics and the 2.6.1 safety decision

The recovered legacy UI models vacation mode using field 49 and only enters from `systemMode == 0`; it leaves from stable `systemMode == 8`. It also reveals the legacy progression `0 -> 3 -> 7 -> 2 -> 8` and uses 25% of the normal slow-wash duration for the vacation brine-draw progress display.

However, that is application/codec evidence, not current-firmware hardware evidence. On the project's physical G6, v2.6.0 sent a direct local field-49 write, received a transport/protocol ACK, then repeatedly performed fresh reads. `vacationPattern` remained `false` until timeout.

Consequently v2.6.1:

- continues reading field 49;
- keeps field-49 encoding in the reusable F79D codec for interoperability research;
- does **not** mark field 49 `HARDWARE_WRITE_VERIFIED`;
- removes the writable Home Assistant Vacation mode switch;
- retains the read-only semantic Vacation status sensor;
- sends no guessed multi-field/mechanical sequence.

Read-only semantic state remains:

- `off`: field 49 false;
- `preparing`: field 49 true, station not 8;
- `active`: field 49 true, station 8.

Stable `vacation + station 8` is not considered mechanical activity for adaptive polling. Transition phases remain active-poll states.

## System-close reasons

Known legacy UI reason codes for field 12:

| Raw | Stable semantic key |
|---:|---|
| 257 | `manual_close` |
| 513 | `leak_detected` |
| 769 | `continuous_flow_timeout` |
| 1025 | `flow_rate_exceeded` |

The raw numeric code is always retained.

## Safe Home Assistant write surface

The protocol codec knows how to serialise more fields than Home Assistant exposes. The generic administrator `write_fields` service is deliberately limited to reversible configuration fields and applies range/unit validation. Fields 34 and 49 are excluded from that generic service. Field 49 has no purpose-specific HA writer in v2.6.1 because its local action is not physically verified on the tested current controller.

## Hardware write verification status

Currently verified locally end-to-end:

- field 4 — device clock;
- field 6 — continuous-flow limit;
- field 10 — regeneration trigger time;
- field 43 — salt addition;
- field 47 — raw-water hardness.

Pending/revalidation required:

- field 7 — flow cutoff, after the 2.6 LE correction;
- field 34 — mechanical regeneration/state-machine controls beyond the specifically tested behavior;
- field 49 — **direct local write disproved on the tested G6; no HA control until a correct local sequence is physically verified**.

## Field 52 polling

The normal state query remains fields 1..51. Field 52 is read separately and cached by the Ypsilon integration. That is an integration polling policy, not part of the generic Runxin transport contract.
